<nav class="w-full bg-white border-b border-gray-200 shadow-sm px-4 md:px-8 z-50 sticky top-0">
  <div class="max-w-7xl mx-auto h-16 flex justify-between items-center">
    <div class="flex items-center w-full justify-between">

      <h1 class="text-xl font-bold">Shopy</h1>

      <div class="flex items-center gap-4 relative" data-controller="dropdown" data-action="click@window->dropdown#hide">
        <%# <%= render "admin/components/share_store" %> %>
        <button class="cursor-pointer" data-action="dropdown#toggle">
          <%= user_avatar(current_user.email_address, "#1b69d8", "white") %>
        </button>
        <%# <%= render "admin/components/dropdown" %> %>
      </div>

    </div>
  </div>
</nav>
